

const firstName = "andres";
const lastName = "jimenez";

export const fullName = `${firstName} ${lastName}`; // se puede expecificar el tipo de variable poniendo :string pero no es neesario
//export const fullName:string = `${firstName} ${lastName}`; 
//console.log(`${2 + 1} mauricio`);

//console.log(`${firstName} ${lastName}`);

//console.log(fullName)